name: GitHub Pages
on:
  workflow_dispatch: ~
  release:
    types:
      - created
      - edited
      - deleted
      - published
      - unpublished

jobs:
  run:
    uses: danielskeenan/mobile_sacn/.github/workflows/gh-pages.yml@pages
